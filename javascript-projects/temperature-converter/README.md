## Temperature Converter 

https://roadmap.sh/projects/temperature-converter

Overview:

In this project, I created a temperature converter that allows users to enter a temperature value, select the unit to convert from, and select the unit to convert to. The "Convert" button is only enabled when all three fields are filled in. Once the user clicks "Convert", the tool displays the converted temperature.